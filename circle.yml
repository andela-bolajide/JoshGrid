dependencies:
  override:
    - yarn install

general:
  build_dir: src

machine:
  test:
    override:
      - yarn test
  timezone:
    Africa/Lagos # Set the timezone
  node:
    version: 6.11.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
