dependencies:
  override:
    - yarn install

machine:
  node:
    version: 6.11.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

  test:
    override:
      - yarn test
